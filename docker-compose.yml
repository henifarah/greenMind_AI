version: "3.9"

services:

  qdrant:
    image: qdrant/qdrant:v1.10.0
    container_name: greenmind_qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
